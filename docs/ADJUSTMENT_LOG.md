# Axiom Adjustment Log

- **Phase B 系統整合：移除裝備系統、天賦樹擴展至 72 節點 (v0.0.1-beta-6f20052):** (1) 完整移除裝備系統（`src/game/equipment.ts`、`src/scenes/equipment.ts`、`tests/equipment.test.ts`、`EquipmentLoadout` 型別、storage/adapter 相關方法）。(2) 天賦樹從 6 節點擴展至 72 節點（三分支各 24 節點）：Survival 分支涵蓋 HP / iframe / 速度 / 拾取半徑；Offense 分支涵蓋傷害 / 暴擊 / 射速 / 彈速，含 `projectilesAdd` 與 `pierceAdd` 核心節點（取代原裝備效果）；Efficiency 分支含 6 個 `skillPointsAdd` 核心節點（全解鎖後共 120 技能點）。(3) 新增 `TalentEffectKind`：`speedMul`、`pickupRadiusMul`、`projectileSpeedMul`、`periodMul`、`pierceAdd`、`projectilesAdd`、`skillPointsAdd`；乘法效果以 delta 累加方式於 run 開始以 `(1 + delta)` 套用至 world。(4) `upgradeTalent()` 新增 `skillTree` 可選參數，技能點核心節點升階時直接賦予 `SkillTreeState.skillPoints`。(5) 移除 `fullEquipment` 成就判斷（保留 AchievementId 以相容存檔）。驗證結果：`npm test`（341/341）與 `npm run build` 皆通過。

- **Phase A 系統整合：技能整合進職業系統 (v0.0.1-beta-6674b18):** 完成技能與職業系統的整合。(1) `ClassNodeDef` 新增 `unlocksSkill?: PrimalSkillId` 欄位；AXIS T1→barrage、T2-A→overload、T2-B→shadowClone；WING T1→axisFreeze、T2-A→timeStop；MIRROR T1→reflectShield、T2-A→lifestealPulse。(2) `classes.ts` 新增 `getClassUnlockedSkills(slot)` 函式，回傳角色槽已晉升節點的技能清單。(3) `createActiveSkillStates()` 簽名改為接受 `PrimalSkillId[]`，由職業節點提供而非全域 SkillTreeState。(4) 移除 `drawPrimalSkill()` 抽卡機制及 `isSkillUnlocked()` boss 門檻；`PrimalSkillDef` 移除 `unlockAfterBoss` 欄位。(5) `BOSS_LOOT_TABLE` 移除 core 項目（weight 重新分配至 points/skin/skillPoints），`BossChestReward` 移除 `core` 欄位，`rollBossChestReward` 同步更新。(6) SkillTreeScene 移除抽卡 UI，改為展示職業解鎖狀態與升級介面；`firstPrimalSkill` 成就觸發移至職業升階 handler。(7) 更新 `tests/skills.test.ts`、`tests/rewards.test.ts`、`tests/unlocks.test.ts`。驗證結果：`npm test`（346/346）與 `npm run build` 皆通過。

- **Class Creation Phase C — ClassCreationScene UI 上線 (v0.0.1-beta-d5c3c48):** 完成 Class Creation 系統的完整 UI 場景。(1) 新增 `src/scenes/classCreation.ts`（ClassCreationScene）：槽位切換列（最多 6 個角色 + 建立按鈕）、三分支職業樹（T0→T1→T2[A/B]→T3[A/B]，依轉職狀態顯示 active / past / next / locked / inactive）、升階確認 modal（含分支選擇、成本顯示、確認／取消）、建立新角色 modal（lineage 選擇器 + 費用確認）、底部被動加成摘要 + 重置按鈕。(2) `src/icons.ts`：新增 `iconLineageAxis`、`iconLineageWing`、`iconLineageMirror`、`iconClassCreate` 四個 SVG 圖示。(3) `src/style.css`：新增 `.cc-*` CSS class 族群（slot tabs、lineage header、tree track、branch pair、promote modal、create modal 等）。(4) `src/main.ts`：`classCreation` case 從 Phase B bridge（StartShapeSelectScene）改為完整 ClassCreationScene，接線 onPromote / onReset / onCreateSlot / onSelectSlot 回呼，並同步 activeStartShape 與 active character lineage。(5) `AGENTS.md` 模組索引新增 classes.ts / classCreation.ts 條目。驗證結果：`npm test`（352/352）與 `npm run build` 皆通過。

- **ROADMAP 職業（創造）系統 Phase A+B 上線 (v0.0.1-beta-8efd422):** 完成 Class Creation 系統的資料層與 run 整合。(1) `src/game/data/types.ts` 新增 `ClassLineageId`、`CharacterSlot`、`CharactersState`、`FusionRecord`（placeholder）、`FusionState`（placeholder），`PlayerProfile` 加入 `characters` 與 `fusion` 欄位，SCHEMA_VERSION 升至 3。(2) 新增 `src/game/content/classes.ts`：三大職系 AXIS / WING / MIRROR（各對應 triangle / square / diamond 起始形狀）、24 個 ClassNodeDef（T0–T3，其中 T2/T3 各有 2 分支）、CHARACTER_SLOT_COSTS（6 槽遞增：0/300/700/1500/3000/5500 pts）、轉職門檻（stageClear + points + Boss 碎片）。(3) 新增 `src/game/classes.ts`：純函式 `classPassiveBonuses()`（加法累積/乘法累積）、`canPromoteClass()`、`promoteClass()`、`resetCharacterClass()`（退 points，不退碎片）、`canCreateCharacterSlot()`、`createCharacterSlot()`、`lineageToStartingShape()`、`startingShapeToLineage()` 等。(4) `src/game/storage.ts` 加入 migration：舊存檔 `activeStartShape` 自動轉為 `characters.slots[0].lineage`。(5) `src/main.ts`：run 開始時套用 `applyClassPassiveBonuses()`（additive + periodMul/speedMul multiplicative via effectEngine），結算時套用 `classMeta.pointRewardMul` / `fragmentRewardMul`；`startRun()` 優先使用 active character lineage 決定起始形狀；主選單 `classCreation` 操作同步 character lineage。(6) `src/scenes/mainMenu.ts`：action kind `startShape` 改名為 `classCreation`，按鈕標籤改為「Class Creation」。(7) 新增 `tests/classes.test.ts`（33 tests）。驗證結果：`npm test`（352/352）與 `npm run build` 皆通過。

- **天賦樹介面視覺重構（參考圖對齊） (v0.0.1-beta-7544172):** 優化 Talent Tree 介面以貼近參考圖風格。(1) `TalentScene` 改為單分支聚焦檢視，新增 survival/offense/efficiency 分支切換列，並在切換時保留既有升級/重置邏輯。(2) 天賦節點改為幾何卡片視覺，加入節點圖示、加成摘要與底部等級膠囊，強化 selected/locked/maxed 狀態辨識。(3) 重新佈局底部操作區，新增 points available 卡片與「Reset Tree (free)」主操作按鈕，整體資訊層級更接近目標設計。驗證結果：`npm test` 與 `npm run build` 通過。

- **天賦樹 UI/UX 樹狀節點與編輯流程優化 (v0.0.1-beta-2df71fa):** 重做 Talent Scene 互動流程。(1) 改為 branch 分區的樹狀節點視覺，點擊節點才在下方展開 detail modal，並加入 smooth focus + smooth open 動畫，降低定位成本。(2) 新增 survival / offense / efficiency 三種 branch 專屬 SVG icon，取代純文字辨識。(3) detail modal 改為可先在草稿狀態自由加減等級，並以 save/cancel 套用或放棄，避免誤觸直接扣資源；數值改以 tag 呈現（等級、加成、資源變化）降低閱讀負擔。(4) 天賦重置改為免費，移除 basic/elite 素材消耗邏輯與相關測試假設。驗證結果：`npm test` 與 `npm run build` 通過。

- **ROADMAP 成長（天賦）系統上線 (v0.0.1-beta-247ef8b):** 完成 Talent Growth roadmap 子項。(1) 新增 `src/game/content/talents.ts` 定義三條天賦支線（生存/輸出/資源效率）與節點成本、前置條件、每級加成 schema。(2) 新增 `src/game/talents.ts` 提供升級、重置（消耗 basic/elite 素材並退還已投入 points）、加成彙總與投入點數計算邏輯。(3) `PlayerProfile` 新增 `talents` 持久化狀態，`storage.loadProfile()` 補上舊存檔遷移預設值。(4) 新增 `TalentScene` 並於主選單加入 Talents 入口，可升級/重置並顯示即時加成與資源。(5) run 啟動時套用生存/輸出天賦至 avatar 基礎屬性；run 結算套用資源效率天賦到 points 與 fragments 收益，採保守比例以維持前期構築壓力。驗證結果：`npm test` 與 `npm run build` 通過。

- **Fragment 材料上限與溢出點數轉換 (v0.0.1-beta-5f069a7):** 完成 Fragment Drop roadmap 子項。(1) `fragments.ts` 新增 `FRAGMENT_MATERIAL_CAP=9999` 與 `applyFragmentGainWithCap()`，統一處理單一素材上限與溢出點數轉換（依該素材 `sellPrice` 計算）。(2) `main.ts` 結算入帳改為逐素材套用 cap，超出份額自動折算為 points；Shop 的碎片購買也套用相同規則，滿倉時不再增加素材而改為回收點數。(3) `tests/rewards.test.ts` 新增 cap 與 overflow 轉點測試，覆蓋 9999 上限與 boss 素材轉換邏輯。(4) `ROADMAP.md` 同步標記「材料上限與溢出轉換規則」完成，並註記保底機制目前暫不設計。驗證結果：`npm test` 與 `npm run build` 通過。

- **Boss 擊殺後開獎結算流程 (v0.0.1-beta-f29d5cf):** 修正 boss 擊殺後直接結算導致無法取得 boss 碎片的流程。(1) `play.ts` 新增 boss 死亡演出時間窗（約 1.1s）與殘留碎片自動收取，避免最後一波立即跳轉遺失掉落；boss 碎片改由擊殺後「開獎」流程發放。(2) `rewards.ts` 新增 `rollBossChestReward()`（白70%/藍20%/深紅10%）與對應碎片/晶核機率（藍 10%、深紅 60%）；`RunResult` 新增 `bossChestReward` 結算資料欄位。(3) 新增 `scenes/bossReward.ts` 與 `style.css` 開獎視窗/晶核盒演出，玩家確認後才進入 `EndgameScene`。(4) `main.ts` 串接結算前開獎，將開獎所得 boss 專屬碎片與晶核正確入帳並反映於結算統計。(5) `ROADMAP.md` 同步勾選已完成的 Fragment Drop 子項（普通碎片、Boss 碎片、結算彙總、兌換介面）。驗證結果：`npm test`（312/312）與 `npm run build` 皆通過。

- **結算與圖鑑改為 Enhance 分類展示 (v0.0.1-beta-b04ba0b):** 調整 UI 以符合 Enhance 流程。(1) `endgame.ts` 結算「abilities」改為「enhance」，資料來源改為 run 內 `CardInventory`，僅顯示 icon + `LvN`（移除技能名稱/文字）。(2) `rewards.ts` 與 `play.ts` 移除 run result 的 `abilityIds` 欄位，避免把 primal skill 混入結算持有能力清單。(3) `codex.ts` 改為 shop 類似的 tab 結構（Enemies / Enhance），將原本 Skills 區塊替換為 Enhance 卡牌圖鑑列表。(4) 敵人圖鑑 icon 依 enemy 基礎色盤上色（對齊遊戲內基礎顏色）。驗證結果：`npm test` 與 `npm run build` 通過。

- **掉落物 UI/UX 與圖鑑整合優化 (v0.0.1-beta-b04ba0b):** 完整優化碎片與結算體驗。(1) 新增 `src/game/fragments.ts` 作為碎片 schema（基礎/各菁英/各 Boss 專屬碎片），定義交易價格、解鎖門檻與 metadata；`PlayerProfile.fragments` 擴充 `detailed` 欄位並在 `storage` 加入遷移，`PlayerStats` 新增 `enemyKills` 累計。(2) `icons.ts` 新增敵人與碎片 icon 對照（`ENEMY_GLYPHS` / `FRAGMENT_GLYPHS`）與主選單 `Codex` icon；`play.ts` 將碎片改為實體掉落動畫（噴散→磁吸→自動拾取），並即時更新右上角碎片 HUD（`index.html` + `style.css`）。(3) 暫停畫面 `pause.ts` 新增碎片清單（含 icon 與數量）；商店 `shop.ts` 新增 Fragments 買賣頁（基礎買10賣3、菁英/Boss 依強度點數換算，且分別需要對應擊殺數 30/10 才可購買）。(4) `endgame.ts` 重做結算內容：顯示遊戲時間、持有能力、敵人擊殺統計（icon）、碎片統計（icon），並提供 retry / next stage / main menu；`main.ts` 串接 run summary 與下一關判斷。(5) 主畫面新增圖鑑 `src/scenes/codex.ts`，展示敵人卡（icon + 動畫 + 描述/數值/擊殺數）與能力卡（icon + 動畫 + 描述/等級/數值），`mainMenu.ts` 與 `main.ts` 完成入口。驗證結果：`npm test`（310/310）與 `npm run build` 皆通過。

- **Fragment Drop System (v0.0.1-beta-44168e8):** 實作掉落物系統（Fragment Drop）。(1) `src/game/rewards.ts` 新增 `FragmentKind`、`FragmentTally`、`emptyFragmentTally()`、`basicFragmentsForEnemy()`、`rollFragmentDrops()`：一般敵人掉落基礎碎片（依關卡 stageStrengthMul 比例縮放）；菁英敵人額外掉落 0–2 菁英碎片；Boss 僅掉落 1–5 boss 碎片。`RunResult` 新增 `fragments: FragmentTally` 欄位。(2) `src/game/data/types.ts` 新增 `FragmentInventory` 介面並掛入 `PlayerProfile.fragments`；`defaultPlayerProfile()` 初始值為 `{ basic:0, elite:0, boss:0 }`。(3) `src/game/storage.ts` `loadProfile()` 加入 fragments 欄位遷移（舊存檔自動補 0）。(4) `src/scenes/play.ts` 新增 `runFragments` 累計器，於 `onEnemyKilled` 呼叫 `rollFragmentDrops` 並累積；`buildRunResult()` 帶出 fragments。(5) `src/scenes/endgame.ts` 新增 `fragments?: FragmentTally` 參數，有掉落時在結算畫面顯示「FRAGMENT HAUL」面板（BASIC ×N | ELITE ×N | BOSS ×N）。(6) `src/main.ts` `settleRun()` 將 `result.fragments` 入帳至 `profile.fragments`，並以 `pendingFragments` 傳入兩處 `EndgameScene`；每次 run 開始重置 `pendingFragments`。(7) `tests/rewards.test.ts` 新增 13 個 fragment 相關測試。驗證結果：`npm test`（310/310）與 `npm run build` 皆通過。

- **Stage Extensibility Refactor — unified stage config + wave template compiler (v0.0.1-beta-f914353):** 重構關卡擴充性。(1) 新增 `src/game/content/stages.ts` 作為唯一關卡設定來源（StageConfig / WaveTemplate / SpawnTemplate schema，含 enemyStrengthMul / pointMul / bossId / waves / enhancePool 欄位）。(2) 新增 `src/game/stageCompiler.ts`（compile StageConfig → WaveSpec[]，支援固定種類、帶機率的 enemyTable、batches/interval 批次生成）。(3) 新增 `src/game/weightedSampler.ts`（通用加權隨機抽樣 weightedPick / weightedPickN，供 enemy table 與 enhancePool 共用）。(4) 移除 stageWaves.ts 中手工列寫的 STAGE_1…STAGE_5 陣列，改由 compiler 推導；移除 bosses/registry.ts 的 STAGE_BOSS_ORDER 常數，改讀 STAGE_CONFIGS。(5) rewards.ts / play.ts 的 NORMAL_STAGE_POINT_MUL / NORMAL_STAGE_STRENGTH_MUL 均改從 STAGE_CONFIGS 推導，消除重複定義。(6) cards.ts drawOffer 新增可選 enhancePool 參數，支援關卡專屬加權抽卡；main.ts 在 onWaveCleared 傳入當前關卡的 enhancePool。(7) 新增 tests/stageCompiler.test.ts（23 tests）與 tests/weightedSampler.test.ts（10 tests）。驗證結果：`npm test`（301/301）與 `npm run build` 皆通過。

- **Stage Expansion: 5 new enemies + 2 new bosses + 2 new stages (v0.0.1-beta-87fffd6):** 擴充主線模式至 5 關。(1) 縮短舊關卡：Stage 1→6波、Stage 2→7波、Stage 3→8波（強度系數降為 ×2.0）。(2) 新增 5 隻一般敵人：spiral（7邊形，螺旋逼近）、lance（細菱形，高速衝刺）、prism（三角形 精英，3向射擊）、octo（八邊形 精英，8方爆炸）、shade（4點星 精英，保持距離+追蹤彈）。(3) 新增 Stage 4「GRID」10波（新敵 spiral/lance/prism）與 Stage 5「VOID」12波（新敵 octo/shade）。(4) 新增 Boss：Lattice（"all planes align"，快速網格波動攻擊）與 Rift（"the null set expands"，追蹤彈→旋轉散射→閃現傳送）。(5) 正常模式積分倍率從 [1,2,3] 擴充為 [1,2,3,4,5]，Stage 3 強度 ×2.0 調降。新增成就 clearStage4／clearStage5。驗證結果：`npm test`（268/268）與 `npm run build` 皆通過。

- **Import Save Data Notification Hardening (v0.0.1-beta-8b7614c):** 強化主選單匯入存檔流程的使用者回饋：未選擇檔案時顯示 info toast（Import canceled），JSON 格式錯誤仍顯示 error toast，並新增 runtime 例外攔截，當讀檔或寫入失敗時顯示 `Failed to import save data...` 的 error toast；成功匯入仍顯示 success toast 並返回主選單。驗證結果：`npm test`（264/264）與 `npm run build` 皆通過。

- **UI Notification Unification + Boundary Service Abstraction (v0.0.1-beta-9123346):** (1) 建立 `src/app/notificationService.ts`：純函式 `buildNotification()` 與 DOM toast `showNotification()` 取代所有 9 處 `alert()`（`src/main.ts` ×7、`src/scenes/skillTree.ts` ×2）；success/error/info 三類型，3000ms/5000ms 自動消失，可點擊提早關閉；CSS toast 樣式加入 `src/style.css`；補 `tests/notificationService.test.ts`（11 個測試）。(2) 建立 `src/app/adapters.ts`：定義 `IStorageAdapter`、`IAudioAdapter`、`IMusicAdapter` 三個邊界服務介面；`src/main.ts` 啟動時集中建立具體 adapter 實例並注入 scene 層；`SettingsCallbacks` 新增 `isMuted`/`setMuted` 改為回呼注入，`settings.ts` 移除直接 import `../game/audio`；`PlayCallbacks` 新增 `playSfx` 回呼，`play.ts` 移除直接 import `../game/audio`；`SkillTreeCallbacks` 新增 `notify` 回呼；補 `tests/adapters.test.ts`（12 個測試，以 mock 驗證三個介面契約）。驗證結果：`npm test` 264/264 通過，`npm run build` 成功。

- **Phase 4 Schema-first + SOP (v0.0.1-beta-b794ce5):** 將遊戲內容資料與業務邏輯分離：新增 `src/game/content/{cards,skills,stageThemes,enemies}.ts` 作為 schema-first 內容來源，並讓 `src/game/cards.ts`、`src/game/skills.ts`、`src/game/stageThemes.ts`、`src/game/enemies/registry.ts` 改為引用內容 schema，維持原本 API 與玩法行為不變。新增 `docs/CONTRIBUTING.md` 並補上「新增敵人 / 技能 / Boss / 關卡」SOP。同步更新 `docs/plans/refactor.md` 的 Phase 4 勾選狀態。驗證結果：`npm test`、`npm run build` 通過。

- **Phase 3 UI 職責拆分（部分）(v0.0.1-beta-aca7ba9):** 將暫停畫面 DOM 組裝自 `src/main.ts` 抽離至 `src/scenes/pause.ts`，`main.ts` 改為只保留 pause 狀態與事件控制；新增 `src/scenes/components/{bonusGrid,cardTag,skillButton}.ts` 作為可複用 UI 片段，並讓 HUD 技能按鈕改由 `SkillButton` 元件生成；`DraftScene` 改用 `src/scenes/ui.ts` 的 overlay helpers（`openOverlay` / `closeOverlay` / `createOverlayTitle` / `createCardList`）。驗證結果：`npm test` 241/241 通過，`npm run build` 成功。

- **Phase 2 領域抽象重構 (v0.0.1-beta-e3c4f2b):** 完成 Phase 2 三個子任務。(2-A) 新增 `src/game/effectEngine.ts`，定義 `EquipEffectKind`，以 `applyEffectToWorld()` 統一卡牌與裝備效果套用，並讓 `mirrorBoss.ts` 改透過同一核心 `applyEffectToMirrorSpec()` 套用鏡像效果；新增 `tests/effectEngine.test.ts`。(2-B) 新增 `src/game/enemies/registry.ts` 作為敵人 stats/elite/spawnBehavior canonical 來源，`entities.ts` 改為透過 registry 生成敵人與初始化行為；更新 `docs/ENEMY.md`，並新增 `tests/enemies/registry.test.ts`。(2-C) 新增 `src/game/bosses/runtime/{orthogon,jets,mirror,index}.ts`，將 Boss AI runtime 自 `bossWeapon.ts` 拆分，`bossWeapon.ts` 縮減為純 re-export 入口；新增隔離測試 `tests/bosses/orthogon.test.ts` 與 `tests/bosses/jets.test.ts`。驗證結果：`npm test` 241/241 通過，`npm run build` 成功。

- **Phase 1 重構骨架 (v0.0.1-beta-2abf98a):** 完成 Phase 1 全部四個子任務，無平衡數值改動。(1-A) 建立 `src/game/enemies/kinds.ts`，以 `ALL_ENEMY_KINDS` const tuple 取代 `main.ts`×2 + `play.ts`×1 三處重複陣列。(1-B) 建立 `src/scenes/ui.ts`（`openOverlay` / `initOverlay` / `closeOverlay` / `createOverlayTitle` / `createOverlaySub` / `createBodyScroll` / `createCardList` / `createBackButton`），並更新所有 9 個 Scene 採用共用 helpers，消除約 200 行重複 DOM 代碼。(1-C) 建立 `src/app/achievementChecker.ts` 純函式（`checkRunAchievements`），並以 25 個 Vitest 測試覆蓋，同步更新 `main.ts` 的 `settleRun` 呼叫此函式。(1-D) 建立 `src/app/runContext.ts`，`RunContext` interface 取代 `main.ts` 中的 inline 型別。全部 231 tests 通過，tsc --noEmit 無錯誤。

- **Mirror Boss parallel homing weapon (v0.0.1-beta-8fd40da):** Tracker mirror ability refactored — homing shots are now fired as a completely separate parallel weapon (`mirrorHomingCooldown`/`mirrorHomingPeriod`) instead of replacing the base aimed fan. Base fire pattern always uses `fireAimedFan`. Parallel homing shot fires with `homingAvatar = true` on the projectile and steers toward the avatar. `updateMirrorBossAbilities` signature updated to `(world, avatarId, rng, dt)`. `applyMirrorSpec` now sets both `mirrorHomingPeriod` and `mirrorHomingCooldown`. 2 new runtime tests added for the parallel homing weapon; total tests 206.
- **Mirror Boss full ability mirroring + dev interval (v0.0.1-beta-8fd40da):** Mirror Boss now mirrors player abilities as actual mechanics instead of flat HP. Aegis → boss gets regenerating shield (`enemy.shield`/`mirrorShieldMax`/`shieldRegenPeriod`). Phase Shift → boss gets periodic invincibility (`mirrorDodgePeriod`/`mirrorIframes`). Revenant → boss revives once at 50% HP (`mirrorSecondChance`). Tracker → boss fires homing projectiles (`mirrorHomingShots`/`homingAvatar` on projectile). Added `updateMirrorBossAbilities` system (shield regen + dodge cooldown tick). Collision.ts updated to respect `mirrorIframes` and `mirrorSecondChance`. Added 20 new tests in `tests/bosses/mirrorAbilities.test.ts`. Developer mode enemy spawn interval max raised from 10 → 999 seconds.
- Added documentation for current Axiom enemy roster/behavior (`axiom/docs/ENEMY.md`), card pool (`axiom/docs/CARDS.md`), and primal skills (`axiom/docs/SKILLS.md`).
- Fixed menu BGM restarting when returning from sub-menus (settings, shop, etc.): `showMainMenu()` no longer calls `stopMusic()` before `playMusic('menu')`, letting the crossfade dedup logic keep the track continuous. Tripled all named boss base HP: Orthogon 45→135, Jets 55→165, Mirror 60→180. Updated `docs/ENEMY.md` boss section with per-boss HP reference.
- **Boss HP rebalance & Jets smooth movement:** Jets HP 165→250, Mirror HP 180→400. Jets boss AI fully reworked — all position changes are now smooth velocity-based glides (no teleporting): repositions to dash-start wall, holds 0.5s dash-telegraph line, fast-dashes to opposite wall, then glides through 4 Z-sweep waypoints firing at each arrival. Removed Jets from generic AI seek loop (`updateEnemyAi`); Jets manages its own seek during idle phase. Fixed Orthogon enrage threshold (hardcoded 45→135). Updated all boss AI tests to multi-tick simulation. Updated `docs/ENEMY.md` and `docs/boss-jets.md`.
- Softened Stage 2 and Stage 3 theme palettes and added configurable matte fog overlay rendering for darker stage atmospheres.
- Added normal-mode stage strength multipliers (Stage 2 = 1.5×, Stage 3 = 2.5×) applied to enemy HP, movement speed, contact damage, and enemy weapon damage at spawn-time.
- Fixed equipment overlay overflow so growing lists no longer push the back button out of reach; limited enhancement card equips to one copy per card; correctly applied selected avatar skin during runs; and added pause/resume/restart run controls.
- Added starting-shape progression: cumulative points now unlock Square (Face Beam) and Diamond (Orbit Shard), added a dedicated "Starting Shape" menu page, persisted selected starting shape in profile data, and applied shape-specific starting weapon modes at run start.
- Added 4 Evolution-class draft cards — Aegis (regenerating shield, max 2 / 6s), Revenant (one-shot revive at 50% maxHp), Compact (-25% hitbox), Phase Shift (auto-dodge with 8s cooldown). Refactored avatar-damage resolution into a single helper (dodge → shield → HP → revenant). Mirror Boss reflects each pick as bonus HP or speed.
- Added an in-game Discussions modal and embedded giscus (`giscus.app/client.js`) for Axiom, wired to `zeroLR/axiom` discussions and opened from a new topbar `comments` button.
- Added a fallback link in the Axiom Discussions modal that opens the repository’s GitHub Discussions page directly when in-widget posting fails.
- Added 6 Weapon-class draft cards (Face Beam, Orbit Shard, Tracker, Burst, Sweep, Cannon) and the parallel-weapon slot system: each pick appends a secondary weapon (cap 3) that fires alongside the primary. Introduced 4 new firing modes (homing missile with bounded turn rate, burst-into-fragments, wide 5-shot fan, slow piercing cannon). Mirror Boss reflects weapon picks as flavour-matched stat pressure (extra projectiles / damage / fire-rate / HP).
- Rebalanced normal-mode progression: enemy kills now use documented base points with stage multipliers (Stage 1=1x, Stage 2=2x, Stage 3=3x), draft reroll token cost now increases by +1 per reroll use within a draft, and stage wave counts are now Stage 1=8 / Stage 2=12 / Stage 3=15.
- **Mirror Boss picks fix (v0.0.1-beta-b6db901):** `spawnEnemy` now accepts an optional `picks` parameter (defaults to `[]`). Developer-mode spawns (`spawnDeveloperEnemiesNow`, `createDefaultEnemyStats`) now pass `this.picks` so the Mirror Boss correctly mirrors the player's drafted cards in both develop and normal mode.
- **Skill Tree / Meta expansion (SCHEMA_VERSION 1 → 2):**
  - Expanded Primal Skills from 2 → 5: added Reflect Shield (blocks damage + reflects projectiles, 3s/35s), Barrage (radial burst, 2s/25s), Lifesteal Pulse (AoE damage + heal, 4s/40s).
  - Added MAX_SKILL_LEVEL = 10 cap; upgrade button hidden at max; `upgradeCost()` returns Infinity at cap.
  - Expanded Achievements from 4 → 16 across 4 categories (Progress, Difficulty, Style, Speed).
  - Added 4 new equipment cards: Resilience (+0.3s iframe), Magnet (+20% pickup radius), Piercing Shot (+1 pierce), Multi Shot (+1 projectile).
  - Added 6th equipment slot expansion (slot-6, 2500 pts).
  - Storage layer now migrates v1 skill tree and achievement states to v2 by merging missing keys from defaults.
  - Export/import handles both v1 and v2 save formats transparently.
- Drafted Main Story Mode redesign plan at `docs/plans/main-story-redesign.md`: repositions Normal Mode as a 3-stage 主線模式 with Domain / Theorem framing (Axis → Wing → Mirror), keeps Mirror Boss as the final-stage boss while introducing Orthogon (Stage 1) and Jets (Stage 2) via a new boss registry, gates 5 new cards + 2 new primal skills behind boss clears with linear unlock, and phases the work in 3 shippable PRs. No gameplay code changed yet.
- Unified overlay list-page layout with the shop screen style by giving the middle scroll container a stable ~5-card viewport height, so list length no longer shifts top/header and bottom/back button positioning.
- Pinned overlay list-page header (title + points/sub + tabs) and back button by making `#overlay-inner` occupy full overlay height and hiding its overflow when it wraps an `.overlay-scroll` container; removed the forced `min-height` on `.overlay-body-scroll` so only the middle list scrolls regardless of item count.
- **Card level / upgrade system:** Added run-scoped card levels (`src/game/cardLevels.ts`). Duplicate draft picks merge into the held card and level it up (max Lv 5). Stat-modifier cards scale with diminishing returns per level; multiplicative effects use additive deltas to prevent inflation. Synergy / Evolution / Weapon cards are excluded from levelling for now. Starting equipment cards seed the inventory at Lv 1. In-game HUD shows held cards as small icon + level chips in the top-left corner. Draft screen annotates duplicates with "↑ Lv N" / "level up" / "MAX". Added 15 tests in `tests/cardLevels.test.ts`. Updated `docs/CARDS.md` with level system documentation.
- **Main Story Mode Phase 1 (boss registry + UI framing):** Introduced boss registry system (`src/game/bosses/`): `types.ts` defines `BossId`/`BossDef`/`BossSpec`; `registry.ts` maps stages 0–2 to Orthogon → Jets → Mirror; `mirror.ts`, `orthogon.ts`, `jets.ts` implement `BossDef` (Phase 1: Orthogon & Jets fallback to standard fan pattern). Refactored `play.ts` to use `bossForStage()` instead of hardcoded mirror. Renamed main menu "Normal Mode" → "Main Story". Overhauled stage select with Domain/Theorem labels, linear stage locking (Stage N+1 requires clearing Stage N), and boss info display. Added stage-entry and boss-spawn title-card overlays (monospace, uppercase, fade in/out, non-blocking). Extended `StageTheme` with `bossId`, `domainName`, `theoremLine`. Updated `concept.md` worldview with Domain/Theorem framing and 3-stage boss roster. Created `docs/main-story.md` canonical design doc. Updated `STYLE.md` §4.3 label. Added 8 boss registry tests.
- **Main Story Mode Phase 2 (independent boss AI):** Implemented Orthogon boss AI — 4-axis (cardinal) sweep with telegraph cross-laser; at ≤50% HP adds 4 diagonal axes for 8-way fire; interleaves aimed fan between axis cycles. Implemented Jets boss AI — side-wall dash (telegraph → instant teleport), Z-sweep (4-waypoint rapid fire), aimed fan; at ≤50% HP enrage triggers speed ×1.5 and 8-projectile scatter burst after each action. Updated `bossWeapon.ts` with `bossPattern` dispatch (`standard`/`orthogon`/`jets`). Added boss-specific rendering: Orthogon drawn as thick cross, Jets as downward-facing paper-plane dart. Added boss-specific fill colours (steel grey / sky blue / pink). Added Orthogon axis telegraph line rendering (ramp alpha). Added boss glyph SVG icons (`iconOrthogon`, `iconJets`, `iconMirror`) to `icons.ts`. Excluded bosses from stage-strength scaling (boss stats set by `BossDef.install` only). Added `bossPattern`, `bossPhase`, `bossTimer`, `bossEnraged`, `bossDashTarget`, `bossTelegraphLines` fields to `Enemy` component. Added 9 boss AI tests (163 total).
- **Main Story Mode Phase 3 (unlock system):** Added `src/game/unlocks.ts` with `isCardUnlocked`, `isSkillUnlocked`, `filterUnlockedCards`, `diffUnlocks`, `bossToStageIndex`. Added `unlockAfterBoss?: BossId` field to `Card` and `PrimalSkillDef` interfaces. Added 5 boss-gated draft cards: Axis Lock (+2 damage, gated by Orthogon), Grid Snap (+25% crit, gated by Orthogon), Contrail (burn 1.5 dps/2.5s, gated by Jets), Rebound+ (+2 ricochet, gated by Jets), Recursion (+3 damage, gated by Mirror). Added 2 new Primal Skills: Axis Freeze (stun + axis-snap enemies, gated by Orthogon) and Overload (triple fire rate + 1 self-damage, gated by Mirror). Re-gated existing Time Stop → Mirror, Shadow Clone → Jets. Updated `drawOffer` to filter locked cards via `PlayerStats`. Updated `drawPrimalSkill` to filter locked skills. Skill tree UI now shows boss-gated skills with lock icon + "UNLOCKS: DEFEAT <BOSS>" text. `EndgameScene` now shows "DOMAIN SEALED — UNLOCKED: ..." banner when new items are unlocked by a boss clear. `settleRun` snapshots stats before run and computes `diffUnlocks` at settlement. Added SVG glyph icons for all 5 new cards and 2 new skills. Extended `PrimalSkillId` union and `defaultSkillTreeState` with `axisFreeze`/`overload`; storage migration auto-merges new keys from defaults. Added 14 unlock tests (177 total).
- Merged duplicate levelable card abilities into shared in-run stacks (e.g. Heavy Rounds/Axis Lock, Crit/Grid Snap), with merged stacks now inheriting the lower rarity tier. Draft cards now show projected post-upgrade effect values instead of only base text, and pause overlay now includes live bonus stats plus card holding status list.
- Added a hidden developer mode toggle (tap the main-menu title 7 times within 3 seconds) that unlocks Survival Develop Mode, plus an in-run bottom dev control bar (player/enemy/enhance/skills/start/resume/restart) with runtime player stat/invincibility/enemy-HP-visualization controls, configurable survival enemy spawn + per-kind base stats, manual enhancement application with target level, and skill enable/effect-duration/cooldown overrides.
- Reworked Develop Mode setting actions (`player`, `enemy`, `enhance`, `skills`) from browser prompt/confirm flows to a unified in-game modal form UI with consistent styling, and fixed bottom control behavior by making `start` actively spawn configured enemies and replacing non-responsive `resume` with a working pause/resume toggle.
- **Develop Mode adjustments:** Added range slider bars for all numeric developer form fields (player stats, skill params, enemy stats). Redesigned enemy settings modal with list/add/edit modes — enemies default to an empty list, added via `+ add enemy` button with a kind dropdown; saved enemies appear as cards with edit/delete actions. Develop mode player now starts as a bare avatar without any equipment abilities, skills, or starting-shape loadout from the formal game.
- **Develop Mode modal parity:** Redesigned `enhance` and `skills` developer settings to match enemy-style list/add/edit flows. Both now open dedicated list pages with card-style entries and explicit add/edit/delete actions; enhance entries persist as target-level presets per card during the run, and skills entries persist as explicit enabled-skill configs that are reapplied through a central entry list.
- **Develop Mode UX adjustments:** Enhanced `enhance` settings now show selected ability icon + description with live `LvN` projected values while moving level sliders (add/edit). In develop runs, unmapped equipment chips (e.g. Resilience/Magnet) are no longer shown in HUD. Bottom controls now use a left toggle to switch the right panel between develop setting buttons and live skill activation buttons, and skills config defaults now include all skills (including hidden/gated ones).
- Fixed three UI layout bugs: (1) Pause-screen detail panel was clipped at top/bottom on mobile — changed `#overlay-inner` `max-height` from `calc(100dvh - 32px)` to `100%` so it is always bounded by the game-area, not the full viewport. (2) Shop and Equipment "Enhance" tab option lists overflowed their container — replaced the CSS `:has()` selector (unsupported on Safari < 15.4) with an explicit `.overlay-constrained` class managed in `enter()`/`exit()` of both scenes, and added `overflow: hidden` to `#overlay-inner.overlay-constrained`. (3) Main screen was missing a settings entry-point — added `iconSettings` gear icon, a new `SettingsScene` with Sound and Screen Shake toggles, wired the "Settings" main-menu button to push this scene and persist changes via `saveSettings()`.
- Pause overlay now shows held card abilities as clickable icon+level tags that open a detail modal with full card description and live scaled values, and Develop Mode skill add/edit now shows the selected skill’s icon, description, and live value summary under the adjustment controls.
- Upgraded stage BGM to prebuilt loop assets via Howler: replaced Web Audio synthesized ambient generation with 3 themed 12s looping tracks (`public/music/axis-loop.wav`, `wing-loop.wav`, `mirror-loop.wav`) mapped per stage (AXIS/WING/MIRROR) in `src/game/music.ts`.
- **Scene-aware BGM with crossfade & volume sliders:** Replaced single-stage music system with category-based BGM (`menu`/`level`/`boss`). Menu plays `menu-ludum-dare-30-06.ogg`, each stage plays its own level track, and boss waves crossfade to stage-matched boss tracks (Orthogon/Jets/Mirror). All transitions use 800ms crossfade. Added BGM Volume and SFX Volume range sliders to the Settings screen for independent control, persisted via `GameSettings`.
- **Develop Mode named boss support + boss reference docs:** Extended `EnemyKind` with `'orthogon' | 'jets' | 'mirror'`; `spawnEnemy()` now handles these by spawning a boss entity and calling `BossDef.install()` with base-stats spec. Develop Mode enemy dropdown in `main.ts` includes all three named bosses. `defaultEnemySpawnConfig` and `createDefaultEnemyStats` in `play.ts` include the new kinds. Added `docs/plans/boss/orthogon.md`, `docs/plans/boss/jets.md`, `docs/plans/boss/mirror.md` with full stats, phase descriptions, and tuning constants. Build and all 184 tests pass.
- **Develop Mode save/load slots + per-slot JSON import/export:** Added IndexedDB-backed develop-mode slot storage (10 named slots) with save/load support for all runtime develop settings (player stats and toggles, enemy interval/list, enhance list, skill list). Added a new `save/load` developer control entry that opens a slot manager with per-slot rename, save, load, export, and import actions. Loading a slot restarts into develop mode and reapplies the full saved config to ensure consistent state reconstruction.
